import type { FC } from 'react';

/**
 * Placeholder. The real login form (React Hook Form + MUI) is implemented in Step 7.
 */
export const LoginPage: FC = () => (
  <main data-testid="login-page">
    <h1>Iniciar sesión</h1>
  </main>
);
